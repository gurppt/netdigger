
# Netdigger
GUI Tkinter pour extraire l'audio avec yt-dlp.
Pour pas retaper une ligne de commande à chaque fois.

## Installation
python -m venv .venv && . .venv/bin/activate
pip install -r requirements.txt

## Lancement
python netdigger.py
